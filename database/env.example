# =============================================================================
# DATABASE - PostgreSQL Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# Command: cp env.example .env
# =============================================================================

# PostgreSQL Configuration
DB_NAME=vms_development
DB_USER=admin
DB_PASSWORD=change_this_password_in_production

# Optional: Advanced PostgreSQL Configuration
# POSTGRES_MAX_CONNECTIONS=200
# POSTGRES_SHARED_BUFFERS=256MB
# POSTGRES_EFFECTIVE_CACHE_SIZE=1GB

# =============================================================================
# NOTES
# =============================================================================
# 1. Change DB_PASSWORD before production deployment
# 2. Use strong passwords (min 16 characters, mix of letters, numbers, symbols)
# 3. For distributed deployment, expose ports carefully
# 4. These variables match the docker-compose.yml environment section
# =============================================================================
