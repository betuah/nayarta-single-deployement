# ===========================================
# DATABASE CONFIG
# ===========================================
DB_USER=example_user
DB_PASSWORD=example_password
DB_NAME=example_database

# ===========================================
# MQTT CONFIG
# ===========================================
EMQX_DASHBOARD_USER=example_root
EMQX_DASHBOARD_PASS=example_pass
MQTT_USER=example_mqtt_user
MQTT_PASSWORD=example_mqtt_pass
MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_TLS=false

# ===========================================
# STORAGE CONFIG
# ===========================================
MINIO_ROOT_USER=example_admin
MINIO_ROOT_PASSWORD=example_password
MINIO_APP_USER=example_app
MINIO_APP_PASSWORD=example_app_password
OBJECT_STORAGE_ACCESS_KEY_ID=example_access_key
OBJECT_STORAGE_ACCESS_KEY_SECRET=example_secret_key
OBJECT_STORAGE_DEFAULT_BUCKET=example-bucket

# ===========================================
# ADMIN CONFIGURATION
# ===========================================
ADMIN_NEXTAUTH_URL=http://localhost:8456
ADMIN_SECRET_KEY="example_admin_secret_key"
ADMIN_DATABASE_URL=postgres://example_user:example_password@example_host/example_database

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
FRONTEND_API_BASE_URL=http://localhost:8457/api/v1
FRONTEND_NEXTAUTH_URL=http://localhost:80
FRONTEND_DATABASE_URL=postgres://frontend_user:frontend_password@localhost:5432/example_db
FRONTEND_JWT_PRIVATE_KEY_BASE64="example_base64_encoded_private_key"

# ===========================================
# API CONFIGURATION
# ===========================================
APP_ENVIRONMENT=development
APP_NAME=example-backend
APP_VERSION=1.0.0
BASE_URL=http://localhost:8457/api/v1
HOMEPAGE_URL=http://localhost:80
API_KEYS=example_api_key:example_api_value

OBJECT_STORAGE_ENDPOINT=http://localhost:9100
OBJECT_STORAGE_REGION=us-east-1
OBJECT_STORAGE_USE_SSL=false
OBJECT_STORAGE_USE_PATH_STYLE=true
OBJECT_STORAGE_PROVIDER=minio

FROM_EMAIL=reply@example.com
SMTP_HOST=smtp.example.com
SMTP_PASSWORD=example_smtp_password
SMTP_PORT=587
SMTP_USERNAME=example_smtp_user

FACE_SEARCH_BASE_URL=https://faceapi.example.com
TELEGRAM_API_BASE_URL=https://api.telegram.org
TELEGRAM_BOT_TOKEN=example_telegram_bot_token

VLM_UNDERSTANDING_API_KEY=example_vlm_api_key
VLM_UNDERSTANDING_BASE_URL=https://api.example.com/v2/example
VLM_UNDERSTANDING_MAX_CONCURRENT_JOBS_PER_FILE=1
VLM_UNDERSTANDING_S3_ENDPOINT=https://localhost:9100
VLM_UNDERSTANDING_STALE_JOB_THRESHOLD_HOURS=24
VLM_UNDERSTANDING_WEBHOOK_KEY=example_webhook_key
VLM_UNDERSTANDING_WEBHOOK_URL=http://localhost:8457/api/v1/webhooks/ai-summary

JWT_EXPIRATION_MINUTES=60
REFRESH_TOKEN_EXPIRATION_MINUTES=1440

LOG_COMPRESS=true
LOG_FILE_PATH=/app/logs/app.log
LOG_FORMAT=json
LOG_LEVEL=debug
LOG_MAX_AGE=28
LOG_MAX_BACKUPS=3
LOG_MAX_SIZE=100
LOG_OUTPUT=stdout

# ===========================================
# CLICKHOUSE DATABASE
# ===========================================
CLICKHOUSE_DATABASE=example_db
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PASSWORD=example_clickhouse_password
CLICKHOUSE_PORT=8123
CLICKHOUSE_SECURE=false
CLICKHOUSE_USER=example_clickhouse_user
