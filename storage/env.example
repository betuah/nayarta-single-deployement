# =============================================================================
# STORAGE - MinIO Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# Command: cp env.example .env
# =============================================================================

# MinIO Root Credentials (Admin Access)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# MinIO Application User
MINIO_APP_USER=nayarta_app
MINIO_APP_PASSWORD=nayarta_app_password_change_me

# MinIO Access Keys (for application access)
OBJECT_STORAGE_ACCESS_KEY_ID=nayarta_app
OBJECT_STORAGE_ACCESS_KEY_SECRET=your_secret_key_here_min_8_chars

# MinIO Buckets (comma-separated)
OBJECT_STORAGE_DEFAULT_BUCKET=nayarta-storage

# Optional: MinIO Performance Configuration
# MINIO_STORAGE_CLASS_STANDARD=EC:2

# =============================================================================
# NOTES
# =============================================================================
# 1. Change all default passwords before production deployment
# 2. Use strong passwords (min 16 characters)
# 3. Generate unique access key and secret key
# 4. Secret key must be minimum 8 characters
# 5. OBJECT_STORAGE_* variables are used by the application services
# 6. MINIO_* variables are used by the MinIO service itself
# =============================================================================
