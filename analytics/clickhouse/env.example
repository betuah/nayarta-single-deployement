# =============================================================================
# CLICKHOUSE - Analytics Database Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# Command: cp env.example .env
# =============================================================================

# ClickHouse Authentication
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=beEs4rP@ssW0rD!

# ClickHouse UI Configuration
# (Uses CLICKHOUSE_USER and CLICKHOUSE_PASSWORD from above)

# ClickHouse Ports
# 8124: HTTP Interface (exposed)
# 9000: Native Client Interface (exposed)
# 9009: Interserver Communication (exposed)
# 8081: Nginx Proxy for UI (exposed)

# Optional: ClickHouse Advanced Configuration
# CLICKHOUSE_MAX_MEMORY_USAGE=10000000000
# CLICKHOUSE_MAX_THREADS=8

# =============================================================================
# NOTES
# =============================================================================
# 1. Change CLICKHOUSE_PASSWORD before production deployment
# 2. Use strong passwords (min 16 characters, mix of letters, numbers, symbols)
# 3. For distributed deployment, configure network access carefully
# 4. UI accessible via proxy at http://localhost:8081
# 5. Configure Kafka integration in ./config/kafka.xml if needed
# =============================================================================
